# Polybox (https://mardix.github.io/polybox)
---
name: 
description:
version:
apps:
  - name: yourdomain.com
    runtime: python
    auto_restart: true
    env:
      ENV_KEY: ENV_VAL
      ENV_KEY2: ENV_VAL2
    process:
      web: 
        cmd: app:app
        server_name: yourdomain.com
        workers: 2

